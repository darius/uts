# Run this with working dir = this dir.
# The commented-out lines below are just different Scheme systems running the same program -- just pick one.

# guile load-init.scm
scheme --script load-init.scm
# uts load-init.scm

# Afterwards, new-init.c has the new version of init.c. Currently you have to do the rest by hand:
# mv new-init.c init.c
# make && ./run-test && test/run-test && ./run-bench && corpus/run-corpus
